[project]
name = "project"
version = "0.1.0"
description = "project"
requires-python = ">=3.12"
dependencies = ["fastapi", "uvicorn[standard]", "pydantic"]

[dependency-groups]
dev = ["pytest", "httpx", "ruff"]
